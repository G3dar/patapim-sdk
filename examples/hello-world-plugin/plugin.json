{
  "name": "hello-world",
  "version": "1.2.0",
  "description": "Sample PATAPIM plugin: MCP tools, commands, a toolbar button, and an instruction block",
  "main": "index.js",
  "permissions": ["terminals:read", "notifications"],
  "contributes": {
    "instructionBlocks": [
      { "text": "The hello-world PATAPIM plugin is installed. It exposes the MCP tools `plugin_hello_world_echo` and `plugin_hello_world_count_terminals`." }
    ],
    "commands": [
      { "id": "hello", "title": "Say hello" },
      { "id": "count", "title": "Count terminals" }
    ],
    "toolbarButtons": [
      { "command": "count", "tooltip": "Count open terminals", "icon": "🔢" }
    ]
  }
}
