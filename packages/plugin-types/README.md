# @patapim/plugin-types

Type definitions for PATAPIM plugins are coming to npm. Until then:

- **The plugin system is live** — see [docs/plugins.md](../../docs/plugins.md) for the manifest format and the `patapim` host API.
- The exact runtime contract is published at [reference/host-entry.js](../../reference/host-entry.js).
- A minimal working plugin: [examples/hello-world-plugin](../../examples/hello-world-plugin).
